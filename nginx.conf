server {
    listen 80;
    server_name brand-battles.ru www.brand-battles.ru;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}
